#!/bin/sh
# Choose a random argument using rndi(1).
if [ "$#" -eq 0 ] ; then
    printf >&2 'rnda: No args given\n'
    exit 1
fi
seed=$(rnds)
argi=$(rndi 1 "$#" "$seed") || exit
shift "$((argi-1))"
printf '%s\n' "$1"

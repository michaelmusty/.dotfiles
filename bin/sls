#!/bin/sh
if [ "$#" -eq 0 ] ; then
    for cfg in /etc/ssh_config "$HOME"/.ssh/config ; do
        [ -e "$cfg" ] || continue
        set -- "$@" "$cfg"
    done
fi
if [ "$#" -eq 0 ] ; then
    printf >&2 'sls: ssh_config(5) paths not found, need argument\n'
    exit 1
fi
slsf -- "$@"

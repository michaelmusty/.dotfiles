#!/bin/sh
# Count all descendants of given directories
for dir in "${@:-.}" ; do
    tot=$(find "$dir" -type d \
        -exec sh -c 'cd -- "$1" && cf' _ {} \; |
        cut -f1 | tot)
    printf '%u\t%s\n' "$tot" "$dir"
done

#!/bin/sh
# Count all descendants of given directories; don't follow symlinks
for dir in "${@:-.}" ; do
    printf '%u\t%s\n' \
        "$(find "$dir" -type d -exec cf -o -- {} \; | tot)"
        "$dir"
done

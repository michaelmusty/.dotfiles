#!/bin/sh
# Run a command and ignore specified exit values.
igs=$1,
shift
"$@"
ex=$?
while [ -n "$igs" ] ; do
    ig=${igs%%,*}
    [ "$((ig == ex))" -eq 1 ] && exit 0
    igs=${igs#*,}
done
exit "$ex"
